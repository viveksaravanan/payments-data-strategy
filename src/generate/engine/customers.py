"""Layer 3 — Customer durable state (D16).

Sets per-card attributes that hold across all 90 days: home zone
(weighted by metro residential weights), affluence draw, banner
loyalty weights (D16.1), preference vector + staple SKUs (D16.2),
and the single card identity (D16.3 — tender, network, wallet flag)
with one-token-but-extensible layout.

Stage 4.3 implements; Stage 3 ships a stub.
"""

# Stage 4.3 will implement.
