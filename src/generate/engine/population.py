"""Layer 2 — Population shape (D14).

Assigns each of ~100k cards an activity profile: which segments it
touches and how active it is in each (per-segment trip budget).
Implements the D14.3 intensity tiers (core/regular/occasional per
segment), the D14.4 participation matrix (~32% multi-merchant,
~6% all-three), and the D14.7 cohort tag (established / new-in-window
/ lapsing).

Stage 4.2 implements; Stage 3 ships a stub.
"""

# Stage 4.2 will implement.
