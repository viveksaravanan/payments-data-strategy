data/*.db filter=lfs diff=lfs merge=lfs -text
# Merchant logo PNGs are committed as REGULAR git blobs (not LFS): GitHub
# Pages does not resolve Git LFS — it serves the pointer file, which broke
# the Section 1 map pins. They're tiny (~554 KB total), so plain blobs work
# on both GitHub Pages and the HF Space (which checks them out either way).
